# Pillume 스마트 복약 케어 대시보드 PRD

> Google Stitch 입력용 제품 요구사항 문서  
> 기준 파일: `dashboard.html`  
> 목표: 기존 IoT 기능과 JavaScript를 보존하면서 화면의 정보 구조와 디자인을
> 현대적인 헬스케어 대시보드로 개선한다.

---

## 1. 제품 개요

### 제품명

**Pillume — Smart Medication Care**

### 한 줄 설명

ESP32 스마트 알약 디스펜서, Teachable Machine 동작 인식, WS2812B 무드등,
피에조 긴급 사이렌을 BLE로 연결하여 복약 상태와 기기 기능을 한 화면에서
관리하는 반응형 웹 대시보드.

### 제품 목표

사용자가 복잡한 기술 지식 없이 다음 작업을 빠르게 수행할 수 있어야 한다.

1. ESP32 장치 `ESP_SZ` 연결 상태 확인
2. 오늘 아침·점심·저녁 복약 여부 확인
3. 디스펜서 상태 및 알약 채우기 제어
4. Teachable Machine 모델과 카메라 실행
5. 제스처로 무드등 켜기·끄기 및 긴급상황 발생
6. 네오픽셀 색상과 밝기 직접 조절
7. 날짜별 복약 기록 확인
8. 긴급상황 확인 및 실제 경보 해제
9. AI 도우미에게 앱 사용법과 오류 해결 방법 질문
10. 누락된 복약 기록을 AI 제안 후 사용자 확인으로 보정

---

## 2. 대상 사용자

### 주요 사용자

- 정해진 시간에 약을 복용해야 하는 사용자
- 고령자 또는 복약 알림이 필요한 사용자
- 사용자의 복약 여부를 함께 확인하는 보호자
- ESP32 기반 IoT 프로젝트를 시연하는 학생 및 개발자

### 사용자 환경

- PC Chrome/Edge
- Android Chrome
- HTTPS 또는 `localhost`
- Bluetooth와 카메라 권한 사용
- 데스크톱, 태블릿, 모바일 화면 모두 대응

---

## 3. 핵심 사용자 경험

### UX 원칙

- 첫 화면에서 오늘 복약 상태와 BLE 연결 상태가 바로 보여야 한다.
- 의료 서비스처럼 신뢰감이 있으면서 지나치게 병원처럼 차갑지 않아야 한다.
- 주요 버튼은 초보자도 용도를 바로 이해할 수 있는 문구를 사용한다.
- 연결 전 사용할 수 없는 기능은 비활성화 상태가 명확해야 한다.
- 정상·주의·긴급 상태는 색상뿐 아니라 텍스트와 아이콘으로 함께 구분한다.
- 긴급상황은 어떤 카드보다 높은 시각적 우선순위를 가져야 한다.
- 하드웨어 제어 기능은 장식보다 조작 정확성과 상태 피드백을 우선한다.
- AI가 시스템을 직접 변경하는 것처럼 보이지 않게 하고, 복약 기록 변경에는
  반드시 별도의 확인 단계를 둔다.
- AI 도우미를 처음 열면 OpenAI API 키 연결 화면을 먼저 표시한다. 키는
  현재 탭 메모리에만 유지하고 파일·소스·localStorage에 저장하지 않는다.

### 원하는 감성

- 따뜻하고 안심되는 스마트 헬스케어
- 깔끔하고 현대적인 IoT 대시보드
- 부드러운 라운드 카드와 충분한 여백
- 친근하지만 장난스럽지 않은 분위기
- 고령자도 읽기 쉬운 명확한 대비와 큰 핵심 정보

### 권장 디자인 키워드

`calm`, `warm healthcare`, `smart home`, `trustworthy`, `minimal`,
`soft rounded cards`, `accessible`, `clean data dashboard`

---

## 4. 비주얼 디자인 방향

### 컬러 시스템

- 기본 배경: 따뜻한 아이보리 또는 아주 옅은 세이지
- 주요 색상: 딥 세이지 그린
- 보조 색상: 라이트 라임, 민트, 부드러운 블루
- 성공: 그린
- 주의/미복용: 앰버 또는 오렌지
- 긴급: 선명한 레드
- 비활성: 중성 회색

긴급상황 외에는 강한 빨간색을 사용하지 않는다.

### 타이포그래피

- 한국어 가독성이 높은 Sans Serif
- 제목과 수치에는 명확한 굵기 차이 사용
- 작은 보조 텍스트도 모바일에서 읽을 수 있는 크기 유지
- 권장 글꼴: `Noto Sans KR`, `Pretendard`, `Inter`, `DM Sans`

### 형태

- 카드 모서리: 16–24px
- 입력 필드와 버튼: 10–14px
- 카드 간격: 최소 16px
- 그림자는 약하고 부드럽게 사용
- 복약 상태와 장치 상태에는 간단한 아이콘 또는 상태 점 사용
- 별도의 이미지나 큰 일러스트는 필수 아님

---

## 5. 화면 정보 구조

### 5.1 상단 헤더

필수 요소:

- Pillume 로고 또는 텍스트 로고
- 보조 문구: `ESP_SZ smart care`
- 복약 캘린더 버튼
- BLE 연결 상태 배지
- `ESP_SZ 연결` 버튼

연결 상태:

- 연결 전: 회색 배지, `BLE 연결 대기`
- 연결 중/완료: 초록 상태 점, `ESP_SZ 연결됨`
- 연결 버튼은 페이지에서 가장 쉽게 찾을 수 있어야 한다.

### 5.2 히어로/요약 영역

핵심 문구 예시:

> 오늘의 약, 잊지 않게.

보조 문구:

> 아침·점심·저녁 복약과 모션 무드등을 한 화면에서 확인하세요.

선택적으로 다음 정보를 요약 카드로 표현한다.

- 오늘 복약 완료 횟수 `0/3`
- 현재 대기 중인 약
- BLE 연결 상태

### 5.3 오늘의 복약 카드

세 개의 복약 행을 표시한다.

| 구분 | 시간 | 상태 |
|---|---|---|
| 아침 약 | 07:00–10:00 | 대기 / 복용 가능 / 복용 완료 |
| 점심 약 | 11:00–13:00 | 대기 / 복용 가능 / 복용 완료 |
| 저녁 약 | 15:00–17:00 | 대기 / 복용 가능 / 복용 완료 |

표현 원칙:

- 복용 완료: 초록색 체크와 `복용 완료`
- 현재 시간대: 강조 배경과 `복용 가능`
- 아직 시간이 아님: 중성 색상과 `대기`
- 상태는 색상만으로 구분하지 않는다.

### 5.4 장치 상태 요약

다음 값을 작은 상태 카드 또는 한 줄 요약으로 표시한다.

- 무드등 ON/OFF
- 대기 약 횟수
- 현재 서보 각도
- 센서 감지 방식 `1초 유지`

### 5.5 알약 채우기 카드

필수 정보:

- 총 3칸
- 서보 위치: `55° → 110° → 165° → 0°`
- 현재 채우기 단계
- `알약 채우기 시작` 또는 `다음 칸으로 이동` 버튼
- BLE 미연결 시 비활성화
- 채우기 모드에서는 IR 자동 배출이 잠긴다는 안내

가능하다면 3개의 원형 단계 표시기로 진행 상태를 보여준다.

### 5.6 Teachable Machine 설정 카드

필수 필드:

- 모델 URL
- 클래스 이름 입력
- 기본값: `1,2,3`

클래스 매핑:

- 클래스 `1` 또는 `Class 1`: 무드등 켜기
- 클래스 `2` 또는 `Class 2`: 무드등 끄기
- 클래스 `3` 또는 `Class 3`: 긴급상황

필수 버튼:

- 설정 저장
- 모션 시작
- 카메라 종료

HTTPS 또는 localhost에서만 BLE와 카메라가 작동한다는 안내를 표시한다.

### 5.7 카메라 및 예측 카드

필수 요소:

- 카메라 미실행 플레이스홀더
- 카메라 영상 영역
- 현재 예측 클래스
- 예측 확률
- 카메라 시작·종료 상태

동작 명령은 예측 확률 85% 이상이 5프레임 연속 유지될 때 전송된다.

### 5.8 LED 색상 및 밝기 카드

필수 조작:

- LED 색상 선택기
- 색상 미리보기
- 밝기 슬라이더 1–100%
- 현재 밝기 숫자
- `색상·밝기 적용`
- `무드등 ON`
- `끄기`
- `긴급 사이렌` 시험 버튼

상태 원칙:

- BLE 연결 전 입력과 적용 버튼 비활성화
- 긴급 사이렌 버튼은 다른 버튼과 명확히 구분되는 빨간색 사용
- 색상 미리보기는 선택한 색과 밝기를 시각적으로 표현

하드웨어 동작 우선순위:

1. 긴급 빨간 점멸
2. 서보 작동 중 파란 체이스
3. 사용자가 설정한 일반 무드등 색상

### 5.9 최근 기록 카드

시간순으로 다음 이벤트를 표시한다.

- 복약 완료
- 복약 시간 시작
- 미복용 알림
- 무드등 ON/OFF
- 무드등 색상·밝기 변경
- 알약 채우기 위치 이동/완료
- 긴급상황 감지
- 긴급상황 해제
- 시간 동기화

각 기록에는 이벤트명, 설명, 날짜·시간을 표시한다.

### 5.10 복약 캘린더 모달

필수 요소:

- 연도와 월
- 이전 달/다음 달 버튼
- 7열 달력
- 날짜별 아침·점심·저녁 복약 마커
- 오늘 날짜 강조
- 범례
- 닫기 버튼

마커:

- `아`: 아침
- `점`: 점심
- `저`: 저녁
- 완료된 항목은 초록색
- 기록이 없는 항목은 회색

### 5.11 긴급상황 모달

전체 화면에서 가장 높은 우선순위로 표시한다.

필수 요소:

- 빨간 경고 아이콘
- 제목: `긴급 동작이 감지되었습니다`
- 감지 메시지
- 안내: `확인 버튼을 누를 때까지 빨간 경고등과 사이렌이 계속됩니다.`
- 큰 빨간색 `확인했습니다` 버튼

중요 동작:

- 이 모달은 배경 클릭이나 일반 닫기 버튼으로 닫히면 안 된다.
- `확인했습니다` 버튼이 ESP32에 BLE 해제 명령을 성공적으로 전송해야 한다.
- BLE 연결이 끊겨 있으면 모달을 닫지 말고 연결 필요 안내를 표시한다.
- 확인 전까지 ESP32의 빨간 LED 점멸과 피에조 사이렌이 계속된다.

### 5.12 Pillume AI 도우미

화면 오른쪽 아래에 항상 접근 가능한 플로팅 `AI 도우미` 버튼을 배치한다.
데스크톱에서는 오른쪽 아래 패널, 모바일에서는 화면 폭에 맞춘 바텀시트에
가까운 형태로 열리게 한다. 긴급상황 모달보다 z-index가 낮아야 한다.

#### 최초 실행: API 키 연결 화면

- AI 도우미를 처음 열면 채팅창보다 OpenAI API 키 연결 화면을 먼저 표시
- 비밀번호 형태의 API 키 입력 필드
- `키 연결하고 시작` 버튼
- 키가 현재 브라우저 탭의 메모리에만 유지된다는 설명
- 파일, HTML, 소스 코드, `localStorage`에 저장하지 않는다는 안내
- 공개된 키를 폐기하고 새 키를 사용하라는 보안 안내
- API 키 원문이 화면에 다시 노출되지 않도록 처리

#### 채팅 화면

- 헤더: `Pillume AI`, 보조 문구 `질문 안내 · 기록 보정`
- 사용자 메시지와 AI 메시지를 명확하게 구분한 말풍선
- 질문 입력창과 전송 버튼
- 답변 생성 중 로딩 상태
- `키 지우기` 기능
- 사용 예시:
  - `아침 약을 먹었는데 기록이 안 됐어`
  - `Teachable Machine 모델 URL은 어디서 바꿔?`
  - `ESP_SZ가 연결되지 않는데 어떻게 해야 해?`

#### 복약 기록 보정

- AI가 이미 복용했지만 누락된 기록을 판단해 확인 카드를 제안할 수 있음
- 카드에 날짜와 아침/점심/저녁 시간대를 명확히 표시
- `확인하고 기록`과 `취소` 버튼 제공
- 사용자가 확인하기 전에는 절대로 복약 완료로 저장하지 않음
- 확인 완료 후 캘린더와 오늘의 복약 상태에 즉시 반영
- 이미 같은 날짜와 시간대 기록이 있으면 중복 저장하지 않음

#### AI 권한 제한

- AI는 BLE, 서보모터, IR 센서, WS2812B, 피에조, 긴급 경보를 직접 제어하지 않음
- AI는 Teachable Machine URL이나 클래스 설정을 직접 바꾸지 않음
- AI는 사용법과 오류 해결 방법을 설명하고 수동 복약 기록만 제안함
- 하드웨어 또는 시스템을 변경했다고 거짓으로 표시하지 않음
- 복약 용량이나 의료 판단은 수행하지 않음

#### 연결 및 오류 상태

- AI 요청은 기존 `/api/assistant` 서버 API를 그대로 사용
- API 키는 `X-OpenAI-Key` 요청 헤더로 기존 서버에 전달
- `file://` 직접 실행 시 AI가 차단될 수 있으므로 `localhost` 또는 HTTPS
  환경이 필요하다는 안내를 유지
- 네트워크 오류, 잘못된 API 키, API 사용 한도 오류를 사용자 친화적인
  한국어 메시지로 표시
- AI 오류가 발생해도 BLE와 다른 IoT 기능은 계속 사용할 수 있어야 함

---

## 6. 반응형 요구사항

### 데스크톱

- 2열 카드 구조 권장
- 오늘의 복약 카드가 가장 넓은 영역 차지
- 최근 기록은 전체 너비 사용 가능

### 태블릿

- 중요 카드 1–2열 혼합
- 버튼과 입력 필드 터치 크기 유지

### 모바일

- 모든 카드를 1열로 배치
- 최소 터치 영역 44×44px
- 헤더 버튼이 화면 밖으로 잘리지 않아야 함
- 캘린더 모달은 화면 너비에 맞게 축소
- 긴급 확인 버튼은 엄지손가락으로 쉽게 누를 수 있도록 하단에 크게 표시
- 긴 문구와 상태 값이 겹치거나 잘리지 않아야 함

---

## 7. 접근성 요구사항

- 모든 입력에 연결된 `label` 제공
- 아이콘 전용 버튼에 `aria-label` 유지
- 키보드로 모든 버튼과 입력 조작 가능
- 포커스 표시 제거 금지
- 텍스트 대비 WCAG AA 수준 권장
- 상태를 색상만으로 전달하지 않기
- 모달에 적절한 `role`, `aria-modal` 유지
- 긴급 모달이 열리면 긴급 내용과 확인 버튼이 명확히 읽혀야 함
- `prefers-reduced-motion` 환경에서는 불필요한 웹 애니메이션 최소화

---

## 8. 기능 보존 조건 — 매우 중요

Stitch는 **시각 디자인과 HTML 구조를 개선하되 현재 JavaScript 및 BLE 기능을
삭제하거나 다른 프레임워크로 바꾸면 안 된다.**

### 반드시 유지할 주요 DOM ID

```text
connection
morningState
lunchState
eveningState
light
pending
angle
refillStatus
refillButton
modelUrl
labels
webcam
prediction
lightPreview
lightColor
brightnessValue
lightBrightness
applyLightButton
lightOffButton
logs
calendarOverlay
calendarTitle
calendarMonth
calendar
emergencyOverlay
emergencyMessage
aiLauncher
aiPanel
aiSetup
aiKeyInput
aiChat
aiMessages
aiInput
aiSend
```

필요하면 새로운 클래스와 래퍼는 추가할 수 있지만 위 ID는 삭제하거나
변경하지 않는다.

### 반드시 유지할 JavaScript 함수

```text
connectBle
saveSettings
startCamera
stopCamera
sendMotion
sendLightSettings
sendRefill
markLightDraft
openCalendar
closeCalendar
changeMonth
closeEmergency
toggleAssistant
activateAssistantKey
forgetAssistantKey
assistantEndpoint
renderAssistant
sendAssistant
confirmAssistantDose
cancelAssistantDose
```

기존 `onclick`, `oninput` 연결이 계속 작동해야 한다.

### 반드시 유지할 외부 기능

- Web Bluetooth BLE 서비스 및 characteristic UUID
- Teachable Machine 및 TensorFlow 스크립트
- 브라우저 시간 동기화
- `localStorage` 복약 기록과 설정 저장
- 클래스 `1/2/3` 명령 매핑
- 긴급 해제 명령 `{"type":"emergency_ack"}`
- LED 설정 명령
- 알약 채우기 명령
- 캘린더 렌더링
- `/api/assistant` OpenAI 프록시 연결
- `X-OpenAI-Key` 요청 헤더
- API 키를 탭 메모리에만 보관하고 영구 저장하지 않는 로직
- AI가 제안한 복약 기록의 사용자 확인·취소 흐름
- AI 수동 복약 기록의 중복 방지 및 캘린더 반영

### 산출물 형식

- 실행 가능한 단일 `dashboard.html`
- CSS는 동일 파일의 `<style>` 내부에 포함
- JavaScript는 동일 파일의 `<script>` 내부에 포함
- 디자인 결과물 자체는 별도의 프런트엔드 빌드 과정 없이 실행 가능
- 기존 기능 코드 전체를 포함
- AI를 포함한 전체 기능은 Chrome/Edge의 HTTPS 또는 localhost에서 작동
- Stitch는 백엔드를 새로 만들지 말고 기존 `/api/assistant` 호출을 유지

---

## 9. 비기능 요구사항

- 첫 화면의 핵심 상태는 빠르게 파악 가능해야 한다.
- 외부 이미지가 없어도 레이아웃이 깨지지 않아야 한다.
- 카메라 영상이 카드 영역을 벗어나지 않아야 한다.
- 긴급 모달의 z-index는 모든 요소보다 높아야 한다.
- AI 패널은 일반 카드보다 높지만 긴급 모달보다 낮은 z-index를 사용한다.
- AI API 키는 DOM 텍스트, URL, localStorage 또는 오류 메시지에 노출하지 않는다.
- 비활성 버튼은 클릭 가능한 버튼과 명확히 구분한다.
- 사용자 입력이나 이벤트 메시지를 HTML로 표시할 때 XSS 방지 로직을 유지한다.
- 모바일에서 가로 스크롤이 발생하지 않아야 한다.

---

## 10. 범위에서 제외할 사항

- ESP32 MicroPython 펌웨어 변경
- ESP32 Wi-Fi 연결 추가
- 새로운 AI 백엔드 구현 또는 기존 `/api/assistant` 규격 변경
- 로그인 및 회원가입
- 결제 기능
- 병원 전자의무기록 연동
- DHT 온습도 센서 UI
- 조도 센서 측정 UI
- 기능과 무관한 마케팅 페이지

---

## 11. 완료 조건

다음 항목을 모두 만족해야 디자인 완료로 본다.

- [ ] `ESP_SZ 연결` 버튼과 BLE 상태가 명확하게 보인다.
- [ ] 아침·점심·저녁 복약 상태가 한눈에 구분된다.
- [ ] 알약 채우기 3단계와 현재 서보 상태를 확인할 수 있다.
- [ ] 모델 URL과 클래스 `1,2,3` 설정이 유지된다.
- [ ] 카메라 시작·종료와 예측 결과가 정상 표시된다.
- [ ] LED 색상과 밝기를 조절할 수 있다.
- [ ] 클래스 1은 ON, 클래스 2는 OFF, 클래스 3은 긴급상황으로 작동한다.
- [ ] 캘린더에서 날짜별 복약 기록을 볼 수 있다.
- [ ] 긴급 팝업은 확인 전까지 닫히지 않는다.
- [ ] `확인했습니다`가 실제 BLE 긴급 해제 명령을 전송한다.
- [ ] 오른쪽 아래 AI 도우미 버튼과 반응형 채팅 패널이 표시된다.
- [ ] AI 최초 실행 시 API 키 연결 화면이 먼저 표시된다.
- [ ] API 키가 파일이나 localStorage에 저장되지 않는다.
- [ ] AI 질문, 로딩, 오류 상태가 명확하게 표시된다.
- [ ] 복약 기록은 `확인하고 기록`을 누른 뒤에만 반영된다.
- [ ] AI는 하드웨어와 설정을 직접 조작할 수 없다.
- [ ] `/api/assistant` 및 `X-OpenAI-Key` 연결 로직이 보존된다.
- [ ] 모든 필수 ID와 JavaScript 함수가 보존된다.
- [ ] 데스크톱과 모바일에서 가로 스크롤 없이 사용할 수 있다.
- [ ] 결과물이 단일 실행형 `dashboard.html`이다.

---

## 12. Stitch에 함께 입력할 최종 요청문

아래 문장을 `dashboard.html` 및 이 PRD와 함께 Stitch에 입력한다.

```text
첨부한 dashboard.html을 이 PRD에 맞춰 리디자인해 주세요.

목표는 따뜻하고 신뢰감 있는 스마트 헬스케어 IoT 대시보드입니다.
사용자가 오늘의 복약 상태, ESP_SZ 블루투스 연결, 알약 채우기,
Teachable Machine 카메라, LED 색상·밝기, 최근 기록, Pillume AI
도우미를 쉽고 빠르게 확인할 수 있게 만들어 주세요.

현재 HTML의 BLE, Teachable Machine, 캘린더, 알약 채우기, LED 제어,
긴급 경보 해제, OpenAI AI 도우미 기능은 이미 완성되어 있습니다. 기능을
다시 만들거나 삭제하지 말고 디자인과 정보 구조만 개선해 주세요.

PRD의 '기능 보존 조건'에 적힌 모든 DOM ID, JavaScript 함수, onclick,
oninput, BLE UUID 및 기존 script 로직을 반드시 보존해 주세요.

오른쪽 아래 AI 도우미는 처음 열 때 OpenAI API 키 입력 화면이 먼저
보여야 합니다. API 키는 현재 탭 메모리에만 유지하고 HTML, URL,
localStorage에 저장하지 마세요. 기존 `/api/assistant` 요청과
`X-OpenAI-Key` 헤더를 그대로 유지하세요.

AI는 앱 사용법과 오류 해결을 안내하고, 누락된 복약 기록은 확인 카드로만
제안해야 합니다. 사용자가 `확인하고 기록`을 누르기 전에는 기록을
변경하면 안 되며 BLE·서보·LED·긴급 경보·모델 설정을 직접 조작하면
안 됩니다.

긴급상황 모달은 가장 높은 우선순위로 표시하고, '확인했습니다' 버튼이
BLE emergency_ack 명령을 성공적으로 전송하기 전에는 닫히지 않게 유지해
주세요.

결과는 외부 프런트엔드 빌드 과정이 필요 없는 단일 dashboard.html 파일로
만들어 주세요. AI의 실제 통신은 기존 서버가 담당하므로 백엔드를 새로
만들지 마세요. 데스크톱, 태블릿, 모바일에 반응형으로 대응하고 접근성을
고려해 주세요.
```
